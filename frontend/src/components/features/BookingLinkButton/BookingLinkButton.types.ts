/**
 * BookingLinkButton Types
 */

export interface BookingLinkButtonProps {
  /** Property slug used to generate booking link */
  propertySlug: string;
  /** Property name (for accessibility/logging) */
  propertyName: string;
}
